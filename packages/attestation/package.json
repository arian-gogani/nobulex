{
  "name": "@nobulex/attestation",
  "version": "0.1.0",
  "description": "Behavioral attestation records — aggregate proof chains into portable agent reputation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@nobulex/core-types": "0.2.1",
    "@nobulex/crypto": "0.2.1",
    "@nobulex/verification": "0.2.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^2.0.0"
  },
  "license": "MIT"
}
